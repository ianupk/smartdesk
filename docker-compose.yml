version: "3.9"

services:
  smartdesk:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: smartdesk-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - .env.local
    restart: unless-stopped
